#pragma once
#include <thread>
#include "IOCPWorker.h"

/*
* IOCPSession
* 연결된 클라이언트를 처리
*/

namespace IOCP
{
	class Session : public Worker
	{
	public:
		//	IOCPSession();
		//	~IOCPSession();

//		bool Initialize(SOCKET _socket, SOCKADDR_IN _addr);
	private:

	};
}